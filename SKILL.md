---
name: photo-vintage-print
description: "旧刊新印：将实拍照片转为复古印刷插画或杂志封面。默认完整执行用户验证过的原始 Prompt，保留人物关系与源图配色，呈现轮廓、半调网点和旧纸质感；不用于保真摄影调色或无原片的通用文章封面。"
---

# 旧刊新印 · 照片转复古印刷

忠实执行原配方，不自动担任 Prompt 改写者。作品是插画转译，不是纪实照片。

## 默认路线：原片 + 完整原 Prompt

1. 用户只问评价、方案或 Prompt 时不自动生成。明确要求出图时直接执行，不重复配置问答。
2. 完整读取 [原始 Prompt](references/original-prompt.md)。默认原封不动传给图像工具：不概括、缩写、重排，不追加人物清单、固定油墨、指定标题位置或额外构图锁定。
3. 用 `scripts/prepare_prompt.py --output <独立作品目录>/prompts/01-final.md` 保存与原始 Prompt 逐字节相同的执行文件。将该文件全部内容作为工具 prompt 参数；不要把工作记录、文件元数据或质检规则拼进去。
4. 实际查看并传入用户原照片。原始配方自己的主体、嘴形、动作、构图与配色要求照常有效；不额外把每个后台人物、倒影、杂物都提升为必须完整保留的主体。
5. 读取当前 imagegen Skill 并使用原生图像工具的真实 schema。照片通过引用图片参数传入；路径、尺寸等工作记录单独保存，不变成额外创作指令。仅有传输需要的简短图片角色说明时可注明，并记录与基准的差异，不谎称严格原文执行。
6. 生成后读取 [验收与交付](references/quality-check.md)，分别检查内容与设计。质检规则用于看结果，不默认追加到生成 Prompt。

## 只有明确要求时才修改

用户说“无字”“少做旧”“标题改为……”或“严格保留背景”时，才读取 [最小修改规则](references/print-controls.md)。只改相应段落，保留其他原文及顺序，并保存变更记录。不要每次都重写整套配方。

默认保留原 Prompt 的封面模式，让模型按照片拟标题、组织印刷色彩及排版；不预先固定标题文案、油墨名单或标题位置。用户明确给出的文案必须保留。用户明确要求精确保留现场文字时，说明原配方的源文字移除规则存在冲突，再做对应的最小修改；不能默默忽略用户要求。

“严格保真”是可选加强项，不是默认追加的限制。要求输出仍是真照片或完全不重绘时，应另走摄影后期路线，不用本 Skill 的生成插画冒充保真照片。

## 文件与工具边界

- 优先用可用的高分辨率原片；必要转换另存并保留正确方向和颜色管理，不直接修改图库内部文件。
- 默认一张原片输出一张作品，不擅自将多张拼成封面。样例不默认传入；用户指定风格参考时才传，并限定它只提供视觉语言。
- 用户认可的 BACKSTAGE MAGIC 是设计评判案例，不是每次都要复制的赭红色模板。其构图和嘴形变化也不是自动授权以后改变人物。
- 保存实际原生输出，读取实际尺寸。不把预览或插值放大称为原片级细节；生成存在随机性，不承诺复刻某一张。
- 工具不可用时说明，不私自换收费服务。当前用户要求及更高优先级工具/安全约束始终优先；基准中的“只输出图片”不取消内部质检或隐瞒失败。

## 权限与数据边界

- 只读取用户明确提供的原片、可选参考图和本 Skill 文件；不扫描相册或邻近目录寻找额外素材。
- 使用当前环境提供的原生 imagegen；默认不要求 API 密钥，不调用自建上传脚本，也不把照片发送到其他第三方服务。
- 输出写入独立作品目录并保留原片；不覆盖原照片或既有成品。临时格式转换不是最终交付，应与原生生成结果区分。
- 生成授权不包含发布、投稿、上传图库或社交平台。未经用户明确要求，不执行任何公开发布动作。
- 运行 `scripts/validate_package.py` 可检查封装完整性与原始 Prompt 锁定值；它只读 Skill 文件并在系统临时目录测试复制，不生成作品或联网。

## 来源

[原始配方](references/original-prompt.md)完整保留。宝玉的借鉴只保留在文件组织、可选文字控制、参考角色及版本记录上，不再重写默认视觉配方。许可见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)；本 Skill 不依赖宝玉安装。
